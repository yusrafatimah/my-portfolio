const defaultTheme = createTheme({
  mode: 'light',
  palette: {
    mode: 'light',
  },

  colors: {
    primaryBackground: lightColor['priprimaryBackgroundmary'],
  },
});

export { defaultTheme };
